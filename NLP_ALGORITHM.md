# อัลกอริทึมและทฤษฎี NLP สำหรับระบบจัดการเรื่องร้องเรียน
### Natural Language Processing — Algorithm & Theory Documentation

---

## บทนำ

ระบบจัดการเรื่องร้องเรียนนี้ใช้กลไกการประมวลผลภาษาธรรมชาติ (Natural Language Processing: NLP) เพื่อวิเคราะห์ข้อความที่ผู้ใช้บริการระบุ และทำการจัดประเภทรวมถึงจัดเส้นทางเรื่องร้องเรียนไปยังหน่วยงานที่เหมาะสมโดยอัตโนมัติ กลไกนี้ประกอบด้วย 3 ส่วนหลักที่ทำงานร่วมกัน ได้แก่ การให้คะแนนเชิงคำสำคัญ (Keyword Scoring) การวัดความหมายเชิงเวกเตอร์ (Semantic Embedding) และระบบการเรียนรู้จากประวัติ (Routing Memory)

---

## ภาพรวมสถาปัตยกรรม NLP

```
ข้อความจากผู้ใช้
        │
        ▼
┌─────────────────────────────────────┐
│         Pre-processing              │
│  Tokenization + Stop Word Removal   │
└──────────────────┬──────────────────┘
                   │
       ┌───────────┴───────────┐
       ▼                       ▼
┌─────────────┐       ┌──────────────────┐
│  Keyword    │       │    Semantic       │
│  Scoring   │       │    Embedding      │
│  (3 Tiers) │       │  (Dense Vector)   │
└──────┬──────┘       └────────┬─────────┘
       │                       │
       └───────────┬───────────┘
                   │  รวมคะแนน (Hybrid Score)
                   ▼
        ┌──────────────────┐
        │  Memory Boost    │
        │ (Learning Layer) │
        └────────┬─────────┘
                 │
                 ▼
        ผลลัพธ์: ประเภทและปัญหาที่แนะนำ
```

---

## ส่วนที่ 1 — การเตรียมข้อความ (Pre-processing)

### 1.1 การแบ่งคำ (Tokenization)

เนื่องจากภาษาไทยไม่มีช่องว่างระหว่างคำ ระบบจึงใช้วิธีแบ่งข้อความโดยอาศัยสัญลักษณ์คั่น (delimiter-based splitting) ร่วมกับการกรองความยาวขั้นต่ำ โดย token ที่มีความยาวน้อยกว่า 3 ตัวอักษรจะถูกตัดออก เนื่องจากไม่มีความหมายที่เพียงพอต่อการจำแนกประเภท

### 1.2 การกำจัดคำหยุด (Stop Word Removal)

คำหยุด คือ คำที่ปรากฏในข้อความบ่อยครั้งแต่ไม่ช่วยในการจำแนกความหมาย เช่น "และ", "หรือ", "ของ", "ใน", "ที่" เป็นต้น ระบบกำหนด Stop Word List สำหรับภาษาไทยโดยเฉพาะ และกำจัดออกก่อนขั้นตอนการให้คะแนนทุกขั้นตอน

หมายเหตุด้านการออกแบบ: คำว่า "นักศึกษา" ถูกจัดเป็นคำหยุดเป็นกรณีพิเศษ เนื่องจากหากนำไปคำนวณจะส่งผลให้หน่วยงานที่มีชื่อเกี่ยวกับนักศึกษาได้คะแนนสูงเกินจริงในทุกคำร้อง ซึ่งไม่สะท้อนความตั้งใจของผู้ร้องเรียน

---

## ส่วนที่ 2 — การให้คะแนนเชิงคำสำคัญ (Keyword Scoring)

อิงแนวคิดจากเทคนิค Thai NLP สองระบบ ได้แก่ LexTo+ (dictionary-based longest matching) และ Longan-Tokenize (candidate segment lookup) โดยการให้คะแนนแบ่งเป็น 3 ระดับ (Tier)

### ทฤษฎีเบื้องหลัง — Character N-gram

ก่อนการให้คะแนน ระบบแปลงข้อความทั้งหมดเป็น **ชุดของ substring** (N-gram Set) ซึ่งครอบคลุมทุกความยาวตั้งแต่ 3 ถึง 16 ตัวอักษร การค้นหาว่า substring หนึ่งปรากฏอยู่ในข้อความหรือไม่จึงทำได้ใน **O(1)** แทนที่จะเป็น O(n) จากการสแกนข้อความทีละตัวอักษร

### Tier 1 — ตรงทั้งหมด (Exact Match)

คำสำคัญปรากฏอยู่ในข้อความครบทั้งคำ

| ตำแหน่งที่ตรง | คะแนน |
|---|---|
| หัวข้อ / ประเภทปัญหา (Title) | **+3** |
| รายละเอียด / ที่ตั้ง (Detail) | **+2** |

### Tier 2 — ตรงบางส่วน (Partial Match ≥ 60%)

กรณีที่คำสำคัญไม่ตรงทั้งคำ แต่ substring ที่ยาวที่สุดของคำสำคัญนั้นปรากฏในข้อความ โดยเงื่อนไขคือ substring ต้องครอบคลุม keyword อย่างน้อย 60% และมีความยาวไม่น้อยกว่า 3 ตัวอักษร

$$\text{partialScore} = \frac{\text{matchLength}}{\text{keywordLength}} \times \begin{cases} 2.4 & \text{(Title)} \\ 1.6 & \text{(Detail)} \end{cases}$$

**กลยุทธ์การค้นหา (Sliding Longest-Match):** ระบบค้นหา substring จากยาวไปสั้น (Greedy/Longest-first) เพื่อให้ได้ match ที่มีความหมายมากที่สุดก่อน จากนั้นหยุดทันทีเมื่อพบ match แรก ซึ่งสอดคล้องกับหลักการของ Longest Matching ใน Thai NLP

### Tier 3 — Token คำอธิบายหน่วยงาน (Description Token Match)

คำในคำอธิบายของหน่วยงาน (เช่น "ซ่อมบำรุง", "ระบบไฟฟ้า") ที่ปรากฏในข้อความร้องเรียน

| ตำแหน่งที่ตรง | คะแนน |
|---|---|
| หัวข้อ (Title) | **+1** |
| รายละเอียด (Detail) | **+0.5** |

---

## ส่วนที่ 3 — การวัดความหมายเชิงเวกเตอร์ (Semantic Embedding)

### 3.1 ทฤษฎี — Sentence Embedding

Sentence Embedding คือกระบวนการแปลงข้อความให้เป็น **เวกเตอร์ตัวเลขในปริภูมิหลายมิติ** (dense vector space) โดยข้อความที่มีความหมายใกล้เคียงกันจะถูกแทนด้วยเวกเตอร์ที่อยู่ใกล้กันในปริภูมินั้น ทำให้ระบบสามารถเชื่อมโยงคำที่มีความหมายเดียวกันแต่เขียนต่างกันได้ เช่น "แอร์" ↔ "เครื่องปรับอากาศ", "เน็ต" ↔ "อินเทอร์เน็ต", "ไมค์" ↔ "ไมโครโฟน"

โมเดลที่ใช้: **paraphrase-multilingual-MiniLM-L12-v2** ซึ่งรองรับหลายภาษารวมถึงภาษาไทย และผลิต vector ขนาด 384 มิติที่ผ่านการ normalize ในระดับ L2 แล้ว

### 3.2 Cosine Similarity

การวัดความใกล้เคียงระหว่าง vector สองชุดใช้ **Cosine Similarity** ซึ่งวัดมุมระหว่างเวกเตอร์ ไม่ใช่ระยะทาง เนื่องจาก vector ถูก L2-normalize แล้ว การคำนวณ cosine similarity จึงเทียบเท่ากับ dot product:

$$\text{cosine}(A, B) = \frac{A \cdot B}{|A||B|} = A \cdot B \quad \text{(เมื่อ normalize แล้ว)}$$

ค่าอยู่ระหว่าง **-1 ถึง 1** โดยค่าที่ใกล้ 1 หมายถึงข้อความมีความหมายใกล้เคียงกันมาก

### 3.3 LRU Cache

เนื่องจากการแปลงข้อความเป็น vector มีต้นทุนการคำนวณสูง ระบบจึงจัดเก็บผลลัพธ์ไว้ใน **LRU Cache (Least Recently Used)** สูงสุด 2,000 รายการ เมื่อ cache เต็ม รายการที่ไม่ได้ใช้งานนานที่สุดจะถูกลบออก เพื่อให้ระบบตอบสนองได้อย่างรวดเร็วโดยไม่สิ้นเปลืองหน่วยความจำ

---

## ส่วนที่ 4 — การรวมคะแนน (Hybrid Scoring)

ระบบรวมคะแนนจากทั้งสองวิธีเข้าด้วยกัน ซึ่งแบ่งออกเป็นสองระดับ ได้แก่ ระดับประเภทร้องเรียน และระดับลักษณะปัญหา

### 4.1 สูตรคะแนนระดับประเภทร้องเรียน (Type Score)

$$\text{typeScore} = \text{kwScore} + \text{semScore} \times 12$$

**เหตุผลของน้ำหนัก:** ตัวคูณ 12 ถูกออกแบบให้ semantic score (ซึ่งปกติอยู่ในช่วง 0.1–0.9) สามารถเพิ่มคะแนนได้ในระดับที่มีนัยสำคัญ แต่ไม่มากพอที่จะแข่งขันกับ keyword score ที่ชัดเจนมากได้ ทำให้ keyword ชนะเสมอเมื่อตรงชัดเจน แต่ semantic ช่วยรองรับกรณีที่ผู้ใช้ใช้คำที่ไม่ตรงกับ keyword ในฐานข้อมูล

ประเภทที่ได้คะแนนรวม **≤ 1.5** จะถูกคัดออก เนื่องจากถือว่าไม่มีความสัมพันธ์ที่เพียงพอกับข้อความที่รับเข้ามา (ค่า 1.5 สอดคล้องกับ semantic threshold ≈ 0.125)

### 4.2 สูตรคะแนนระดับลักษณะปัญหา (Issue Score)

$$\text{issueScore} = \text{semScore} \times 0.65 + \frac{\text{kwScore}}{\text{maxKwScore}} \times 0.35$$

ในระดับนี้ semantic score ได้รับน้ำหนักมากกว่า (65%) เนื่องจากชื่อลักษณะปัญหามักมีความหลากหลายทางภาษามากกว่าชื่อประเภท และ keyword score ถูกทำให้เป็นค่าสัมพัทธ์ (normalized) โดยหารด้วยคะแนน keyword สูงสุดในประเภทนั้น เพื่อป้องกันการเอนเอียงจากจำนวน keyword ที่ไม่เท่ากัน

---

## ส่วนที่ 5 — ระบบการเรียนรู้จากประวัติ (Routing Memory System)

### 5.1 แนวคิด

ระบบความจำทำหน้าที่เป็น **Learning Layer** ที่เรียนรู้จากผลลัพธ์จริงในอดีต โดยติดตามว่าแต่ละคำสำคัญ (token) เคยถูกเชื่อมโยงกับหน่วยงานใด และผลการเชื่อมโยงนั้นถูกต้องหรือไม่ ข้อมูลดังกล่าวจะถูกนำมาใช้เพิ่มคะแนน (boost) ให้แก่หน่วยงานที่มีประวัติถูกต้องสูง

### 5.2 โครงสร้างข้อมูล

สำหรับแต่ละ token ระบบจัดเก็บข้อมูลต่อไปนี้:

```
token: "แอร์"
  └── depts:
        ├── "กองอาคารสถานที่": { score: 5.0, count: 6 }
        └── "ศูนย์ดิจิทัลฯ":  { score: -0.5, count: 1 }
```

- **score** สะสมจาก +1.0 ต่อครั้งที่การจัดเส้นทางถูกต้อง และ -0.5 ต่อครั้งที่มีการโอนย้ายไปหน่วยงานอื่น
- **count** นับจำนวนครั้งที่ token นี้ถูกใช้รวมทั้งหมด

### 5.3 สูตรคำนวณ Memory Boost

$$\text{confidence} = \min\!\left(1.0,\ \max\!\left(0,\ \frac{\text{score}}{\text{count}}\right)\right)$$

$$\text{boost}_{\text{dept}} = \sum_{\text{token} \in \text{tokens}} \text{confidence}_{\text{token}} \times W_{\text{boost}}$$

โดย $W_{\text{boost}} = 3$ (ค่าคงที่ที่กำหนดให้หน่วยงานที่เคยถูกต้อง 100% ได้รับ boost สูงสุด +3 ต่อ token)

### 5.4 กลไกการปรับปรุง (Reinforcement)

| เหตุการณ์ | การปรับคะแนน |
|---|---|
| เรื่องร้องเรียนถูกส่งไปยังหน่วยงาน A | บันทึก +1.0 ให้ทุก token จากข้อความร้องเรียน → หน่วยงาน A |
| ผู้ดูแลระบบโอนย้ายจาก A ไป B | บันทึก -0.5 ให้ทุก token → หน่วยงาน A (ลงโทษ) และ +1.0 → หน่วยงาน B (เสริมแรง) |

กลไกนี้สอดคล้องกับหลักการของ **Reinforcement Learning จากข้อมูลจริง (Online Learning)** โดยที่ระบบปรับปรุงตัวเองตามผลป้อนกลับจากผู้ใช้งานโดยตรง ไม่จำเป็นต้องมีการฝึกฝนโมเดลใหม่ (retraining)

### 5.5 การควบคุมคุณภาพ Token

เพื่อให้ token ที่บันทึกมีความหมายที่แท้จริง ระบบกำหนดเกณฑ์ดังนี้

- ความยาวขั้นต่ำ: 3 ตัวอักษร
- กำจัด Stop Words ออกก่อนบันทึก
- จำกัดจำนวน token สูงสุด 8 ตัวต่อเรื่องร้องเรียน โดยคัดเลือกจาก token ที่ยาวที่สุดก่อน (token ยาว = มีความเฉพาะเจาะจงสูงกว่า)

---

## ส่วนที่ 6 — ลำดับการทำงานของระบบจัดเส้นทาง (Routing Pipeline)

เมื่อผู้ใช้บริการยื่นเรื่องร้องเรียน ระบบดำเนินการตามลำดับความสำคัญดังนี้

```
ขั้นที่ 1: มี issueId หรือไม่?
   ├── ใช่ + หน่วยงานเดียว  → ส่งตรงทันที (Direct Routing, confidence = 100%)
   ├── ใช่ + หลายหน่วยงาน  → คำนวณ Keyword Score + Memory Boost → เลือกสูงสุด
   └── ไม่มี issueId → ขั้นที่ 2

ขั้นที่ 2: ประเภทร้องเรียนระบุหน่วยงานตรงหรือไม่?
   ├── ใช่ → ส่งตรง
   └── ไม่ใช่ → ขั้นที่ 3

ขั้นที่ 3: Fallback — คำนวณ Keyword Score กับทุกหน่วยงาน + Memory Boost
   └── เลือกหน่วยงานที่ได้คะแนนสูงสุด
```

---

## สรุปเปรียบเทียบจุดแข็งของแต่ละวิธี

| วิธี | จุดแข็ง | ข้อจำกัด |
|---|---|---|
| **Keyword Scoring** | แม่นยำสูงเมื่อคำตรงกัน, คำนวณเร็ว | ไม่รองรับคำพ้องความหมาย |
| **Semantic Embedding** | รองรับคำพ้อง, ภาษาพูด, การสะกดผิดบางส่วน | ต้องการทรัพยากรคำนวณมากกว่า |
| **Routing Memory** | เรียนรู้จากการใช้งานจริงสะสม, ปรับปรุงตัวเองอัตโนมัติ | ต้องการข้อมูลสะสมก่อนจึงจะมีประสิทธิภาพ |

การรวมทั้งสามวิธีเข้าด้วยกันทำให้ระบบมีความสมดุลระหว่างความแม่นยำ ความยืดหยุ่น และความสามารถในการปรับตัวตามบริบทการใช้งานจริงของมหาวิทยาลัย

---

*เอกสารนี้จัดทำขึ้นเพื่อประกอบการอธิบายกลไก NLP เชิงวิชาการ*  
*มหาวิทยาลัยหัวเฉียวเฉลิมพระเกียรติ — ปีการศึกษา 2568*
