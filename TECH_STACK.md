# เครื่องมือและเทคโนโลยีที่ใช้งานจริงในโครงงาน
### Technology Stack — HCU Online Complaint Management System

---

## สรุปสถานะเครื่องมือแต่ละรายการ

| ลำดับ | เครื่องมือ / เทคโนโลยี | สถานะ | หมายเหตุ |
|---|---|---|---|
| 3.2.1 | JavaScript | ✅ ใช้งานจริง | ภาษาหลักทั้ง Frontend และ Backend |
| 3.2.2 | HTML | ✅ ใช้งานจริง | ผ่าน JSX ใน React |
| 3.2.3 | CSS | ✅ ใช้งานจริง | ไฟล์ App.css, index.css, AuthPages.css |
| 3.2.4 | React.js | ✅ ใช้งานจริง | เวอร์ชัน 18 |
| 3.2.5 | Node.js + Express | ✅ ใช้งานจริง | Backend server พอร์ต 5000 |
| 3.2.6 | Firebase | ✅ ใช้งานจริง | Firestore + Authentication |
| 3.2.7 | LINE Official Account | ❌ ไม่ได้ใช้ | ไม่มีในโปรเจค |
| 3.2.8 | ngrok | ❌ ไม่ได้ใช้ | ไม่มีในโปรเจค |
| 3.2.9 | Visual Studio Code | ✅ ใช้งานจริง | เครื่องมือพัฒนาหลัก |

---

## รายละเอียดเครื่องมือที่ใช้งานจริง

---

### 3.2.1 JavaScript

JavaScript ถูกใช้เป็นภาษาโปรแกรมหลักของโครงงานนี้ทั้งในส่วน Frontend และ Backend ซึ่งช่วยให้ทีมพัฒนาสามารถใช้ภาษาเดียวกันตลอดทั้งระบบ ลดความซับซ้อนในการพัฒนา และอำนวยความสะดวกในการบำรุงรักษาโค้ดในระยะยาว

**การใช้งานในโครงงาน:**
- ฝั่ง Frontend: ควบคุมการโต้ตอบของผู้ใช้งาน การเรียก API และการจัดการสถานะของแอปพลิเคชัน
- ฝั่ง Backend: รับและประมวลผลคำร้องขอ ดำเนินการวิเคราะห์ข้อความด้วย NLP และเชื่อมต่อกับฐานข้อมูล

---

### 3.2.2 HTML (HyperText Markup Language)

HTML ถูกใช้ในการกำหนดโครงสร้างของหน้าเว็บไซต์ทั้งหมด โดยในโครงงานนี้การเขียน HTML ดำเนินการผ่านรูปแบบ JSX ซึ่งเป็นส่วนหนึ่งของ React.js ทำให้โครงสร้างหน้าเว็บและตรรกะการทำงานอยู่ในไฟล์เดียวกัน

**การใช้งานในโครงงาน:**
- แบบฟอร์มยื่นเรื่องร้องเรียน
- ตารางแสดงรายการเรื่องร้องเรียน
- โครงสร้างหน้าแผงควบคุมของผู้ดูแลระบบและเจ้าหน้าที่

---

### 3.2.3 CSS (Cascading Style Sheets)

CSS ถูกใช้สำหรับออกแบบและจัดรูปแบบการแสดงผลของระบบ เพื่อให้ส่วนติดต่อผู้ใช้งานมีความสวยงาม เป็นระเบียบ และใช้งานได้สะดวก โดยโครงงานนี้ใช้ทั้ง CSS ดั้งเดิมร่วมกับ Material UI Component Library

**การใช้งานในโครงงาน:**
- `App.css` — สไตล์หลักของแอปพลิเคชัน
- `index.css` — สไตล์ระดับฐาน (Base styles)
- `AuthPages.css` — สไตล์เฉพาะหน้าเข้าสู่ระบบและลงทะเบียน

---

### 3.2.4 React.js

React.js เวอร์ชัน 18 ถูกใช้ในการพัฒนาส่วนแสดงผล (Frontend) ทั้งหมดของระบบ ด้วยสถาปัตยกรรม Component-based ที่ช่วยให้สามารถแบ่งหน้าจอออกเป็นส่วนย่อย ๆ ที่นำกลับมาใช้ซ้ำได้ และรองรับการอัปเดตข้อมูลแบบทันทีโดยไม่ต้องโหลดหน้าซ้ำ

**การใช้งานในโครงงาน:**
- หน้ายื่นเรื่องร้องเรียน (`ReportPage`)
- หน้าติดตามสถานะ (`CheckStatusPage`)
- แผงควบคุมผู้ดูแลระบบ (`AdminSystem`)
- ระบบจัดการเรื่องร้องเรียนสำหรับเจ้าหน้าที่ (`OfficerSystem`)
- ระบบแจ้งเตือน (`NotificationBell`) และอื่น ๆ

---

### 3.2.5 Node.js และ Express

Node.js ถูกใช้พัฒนาระบบฝั่งเซิร์ฟเวอร์ (Backend) โดยทำงานร่วมกับ Express framework ซึ่งเป็นกรอบการทำงานที่ช่วยในการสร้าง RESTful API ได้อย่างรวดเร็วและมีโครงสร้างที่ชัดเจน เซิร์ฟเวอร์ทำงานบนพอร์ต 5000

**การใช้งานในโครงงาน:**
- `server/server.js` — จุดเริ่มต้นของเซิร์ฟเวอร์
- `server/routes/complaints.js` — API จัดการเรื่องร้องเรียนและ NLP Autofill
- `server/routes/admin.js` — API สำหรับผู้ดูแลระบบ
- `server/utils/departmentRouter.js` — ระบบจัดเส้นทางอัตโนมัติ
- `server/utils/embedder.js` — ระบบ Semantic Embedding
- `server/utils/routingMemory.js` — ระบบความจำการจัดเส้นทาง

---

### 3.2.6 Firebase

Firebase ถูกใช้เป็นโครงสร้างพื้นฐานหลักของระบบ ครอบคลุมทั้งฐานข้อมูลและการยืนยันตัวตน โดยใช้บริการ 2 ส่วนหลัก

**Firestore (Cloud Database)**
ฐานข้อมูลเชิงเอกสาร (Document Database) ที่จัดเก็บข้อมูลทั้งหมดของระบบ ได้แก่

| Collection | ข้อมูลที่จัดเก็บ |
|---|---|
| `users` | บัญชีผู้ใช้งานและบทบาท |
| `complaints` | เรื่องร้องเรียนทั้งหมด |
| `complaintTypes` | ประเภทของเรื่องร้องเรียน |
| `complaintIssues` | รายการลักษณะปัญหา |
| `departments` | ข้อมูลหน่วยงานและ keyword |
| `notifications` | การแจ้งเตือนในระบบ |
| `customIssueRequests` | คำขอปัญหาใหม่รอพิจารณา |
| `routingTokenMemory` | ประวัติการเรียนรู้การจัดเส้นทาง |

**Firebase Authentication**
ระบบยืนยันตัวตนที่จัดการการเข้าสู่ระบบ การลงทะเบียน และการรักษาความปลอดภัยของบัญชีผู้ใช้

---

### 3.2.9 Visual Studio Code

Visual Studio Code ถูกใช้เป็นโปรแกรมแก้ไขโค้ดหลักในการพัฒนาโครงงานนี้ ซึ่งรองรับ JavaScript, React และ Node.js ได้อย่างครบถ้วน รวมถึงมี Extension ที่ช่วยเพิ่มประสิทธิภาพในการพัฒนาและตรวจสอบโค้ด

---

## เครื่องมือที่ระบุไว้แต่ไม่ได้ใช้งานในโครงงาน

### 3.2.7 LINE Official Account
ไม่มีการนำ LINE Official Account มาใช้งานในโครงงานนี้ ระบบการแจ้งเตือนทั้งหมดดำเนินการผ่านระบบ Notification ภายในแอปพลิเคชัน (In-app Notification) ผ่าน Firestore โดยตรง

### 3.2.8 ngrok
ไม่มีการนำ ngrok มาใช้งานในโครงงานนี้ การพัฒนาและทดสอบระบบดำเนินการบน Local Server โดยตรง โดย Frontend ทำงานบนพอร์ต 3000 และ Backend ทำงานบนพอร์ต 5000

---

*เอกสารนี้จัดทำขึ้นเพื่อประกอบการนำเสนอโครงงานและตรวจสอบความสอดคล้องระหว่างเครื่องมือที่วางแผนไว้กับที่ใช้งานจริง*
*มหาวิทยาลัยหัวเฉียวเฉลิมพระเกียรติ — ปีการศึกษา 2568*
